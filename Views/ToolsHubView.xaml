<UserControl x:Class="StreamCommand.Views.ToolsHubView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Background="{DynamicResource AppBackground}">

    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <StackPanel Margin="20" MaxWidth="1100">

            <TextBlock Text="Tools Hub" Style="{StaticResource SectionHeader}" FontSize="18" Margin="0,0,0,4"/>
            <TextBlock Text="Curated streaming tools — click any card to open in your browser"
                       Style="{StaticResource Muted}" Margin="0,0,0,20"/>

            <!-- Overlays & Alerts -->
            <TextBlock Text="OVERLAYS &amp; ALERT EDITORS" Style="{StaticResource Label}" Margin="0,0,0,8"/>
            <UniformGrid Columns="2" Margin="0,0,0,16">
                <Button Tag="https://streamelements.com/overlay-editor" Content="🎨  StreamElements Editor — Drag-and-drop overlay and alert builder" Style="{StaticResource SecondaryButton}" HorizontalContentAlignment="Left" Padding="16,14" Margin="0,0,6,6" Click="Tool_Click"/>
                <Button Tag="https://streamlabs.com/themes"             Content="🎭  Streamlabs Themes — Pre-built overlay packs and alert themes"  Style="{StaticResource SecondaryButton}" HorizontalContentAlignment="Left" Padding="16,14" Margin="6,0,0,6" Click="Tool_Click"/>
                <Button Tag="https://nerdordie.com"                     Content="🤓  Nerd or Die — Premium overlay and widget designs"               Style="{StaticResource SecondaryButton}" HorizontalContentAlignment="Left" Padding="16,14" Margin="0,6,6,0" Click="Tool_Click"/>
                <Button Tag="https://www.own3d.tv/en/overlays"          Content="💎  OWN3D — Stream overlays, alerts, and panels"                    Style="{StaticResource SecondaryButton}" HorizontalContentAlignment="Left" Padding="16,14" Margin="6,6,0,0" Click="Tool_Click"/>
            </UniformGrid>

            <!-- Thumbnails & Graphics -->
            <TextBlock Text="THUMBNAIL &amp; GRAPHICS" Style="{StaticResource Label}" Margin="0,0,0,8"/>
            <UniformGrid Columns="2" Margin="0,0,0,16">
                <Button Tag="https://www.canva.com"          Content="🖼️  Canva — Thumbnail and banner design, streaming graphics"  Style="{StaticResource SecondaryButton}" HorizontalContentAlignment="Left" Padding="16,14" Margin="0,0,6,6" Click="Tool_Click"/>
                <Button Tag="https://www.adobe.com/express/" Content="✏️  Adobe Express — Fast social graphics and thumbnails"      Style="{StaticResource SecondaryButton}" HorizontalContentAlignment="Left" Padding="16,14" Margin="6,0,0,6" Click="Tool_Click"/>
            </UniformGrid>

            <!-- DMCA-Safe Music -->
            <TextBlock Text="DMCA-SAFE MUSIC" Style="{StaticResource Label}" Margin="0,0,0,8"/>
            <UniformGrid Columns="2" Margin="0,0,0,16">
                <Button Tag="https://www.pretzel.rocks"          Content="🎵  Pretzel Rocks — Stream-safe music player with Spotify integration" Style="{StaticResource SecondaryButton}" HorizontalContentAlignment="Left" Padding="16,14" Margin="0,0,6,6" Click="Tool_Click"/>
                <Button Tag="https://www.monstercat.com/player"  Content="🐱  Monstercat — EDM label with stream-safe licensing"                 Style="{StaticResource SecondaryButton}" HorizontalContentAlignment="Left" Padding="16,14" Margin="6,0,0,6" Click="Tool_Click"/>
                <Button Tag="https://www.epidemicsound.com"      Content="🎶  Epidemic Sound — Royalty-free music library"                       Style="{StaticResource SecondaryButton}" HorizontalContentAlignment="Left" Padding="16,14" Margin="0,6,6,0" Click="Tool_Click"/>
                <Button Tag="https://www.soundstripe.com"        Content="🎼  Soundstripe — Unlimited stream-safe music"                         Style="{StaticResource SecondaryButton}" HorizontalContentAlignment="Left" Padding="16,14" Margin="6,6,0,0" Click="Tool_Click"/>
            </UniformGrid>

            <!-- Analytics -->
            <TextBlock Text="CHANNEL ANALYTICS" Style="{StaticResource Label}" Margin="0,0,0,8"/>
            <UniformGrid Columns="2" Margin="0,0,0,16">
                <Button Tag="https://socialblade.com"     Content="📊  Social Blade — Track growth across Twitch, YouTube, and more" Style="{StaticResource SecondaryButton}" HorizontalContentAlignment="Left" Padding="16,14" Margin="0,0,6,6" Click="Tool_Click"/>
                <Button Tag="https://twitchtracker.com"   Content="📈  Twitch Tracker — Detailed Twitch statistics and rankings"    Style="{StaticResource SecondaryButton}" HorizontalContentAlignment="Left" Padding="16,14" Margin="6,0,0,6" Click="Tool_Click"/>
                <Button Tag="https://sullygnome.com"      Content="🔍  SullyGnome — Twitch channel and game analytics"              Style="{StaticResource SecondaryButton}" HorizontalContentAlignment="Left" Padding="16,14" Margin="0,6,6,0" Click="Tool_Click"/>
            </UniformGrid>

            <!-- Chat Bots -->
            <TextBlock Text="CHAT &amp; COMMUNITY" Style="{StaticResource Label}" Margin="0,0,0,8"/>
            <UniformGrid Columns="2" Margin="0,0,0,16">
                <Button Tag="https://nightbot.tv"         Content="🤖  Nightbot — Chat moderation bot with commands and filters"    Style="{StaticResource SecondaryButton}" HorizontalContentAlignment="Left" Padding="16,14" Margin="0,0,6,6" Click="Tool_Click"/>
                <Button Tag="https://moo.bot"             Content="🐄  Moobot — Twitch chatbot for commands and loyalty"            Style="{StaticResource SecondaryButton}" HorizontalContentAlignment="Left" Padding="16,14" Margin="6,0,0,6" Click="Tool_Click"/>
            </UniformGrid>

        </StackPanel>
    </ScrollViewer>
</UserControl>
