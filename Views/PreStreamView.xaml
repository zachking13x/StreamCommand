<UserControl x:Class="StreamCommand.Views.PreStreamView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:models="clr-namespace:StreamCommand.Models"
             Background="{DynamicResource AppBackground}">

    <UserControl.Resources>
        <!-- Must be declared before DataTemplates that reference it -->
        <BooleanToVisibilityConverter x:Key="BoolToVis"/>

        <!-- Checklist item template -->
        <DataTemplate x:Key="ChecklistItemTemplate" DataType="{x:Type models:ChecklistItem}">
            <Border BorderBrush="{DynamicResource SeparatorBrush}"
                    BorderThickness="0,0,0,1"
                    Padding="16,12">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <!-- Checkbox circle -->
                    <CheckBox Grid.Column="0"
                              IsChecked="{Binding IsChecked, Mode=TwoWay}"
                              VerticalAlignment="Top"
                              Margin="0,2,12,0"
                              Cursor="Hand">
                        <CheckBox.Template>
                            <ControlTemplate TargetType="CheckBox">
                                <Border x:Name="Bd"
                                        Width="20" Height="20"
                                        CornerRadius="10"
                                        BorderThickness="2"
                                        BorderBrush="#444"
                                        Background="Transparent"
                                        Cursor="Hand">
                                    <TextBlock x:Name="Check" Text="✓"
                                               Foreground="White"
                                               FontSize="11" FontWeight="Bold"
                                               HorizontalAlignment="Center"
                                               VerticalAlignment="Center"
                                               Visibility="Hidden"/>
                                </Border>
                                <ControlTemplate.Triggers>
                                    <Trigger Property="IsChecked" Value="True">
                                        <Setter TargetName="Bd" Property="Background" Value="{DynamicResource AccentBrush}"/>
                                        <Setter TargetName="Bd" Property="BorderBrush" Value="{DynamicResource AccentBrush}"/>
                                        <Setter TargetName="Check" Property="Visibility" Value="Visible"/>
                                    </Trigger>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter TargetName="Bd" Property="BorderBrush" Value="{DynamicResource AccentLight}"/>
                                    </Trigger>
                                </ControlTemplate.Triggers>
                            </ControlTemplate>
                        </CheckBox.Template>
                    </CheckBox>

                    <!-- Content -->
                    <StackPanel Grid.Column="1">
                        <TextBlock Text="{Binding Label}"
                                   FontSize="13" FontWeight="Medium"
                                   TextWrapping="Wrap">
                            <TextBlock.Style>
                                <Style TargetType="TextBlock">
                                    <Setter Property="Foreground" Value="{DynamicResource PrimaryText}"/>
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding IsChecked}" Value="True">
                                            <Setter Property="Foreground" Value="{DynamicResource MutedText}"/>
                                            <Setter Property="TextDecorations" Value="Strikethrough"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </TextBlock.Style>
                        </TextBlock>
                        <TextBlock Text="{Binding Description}"
                                   Style="{StaticResource Muted}"
                                   TextWrapping="Wrap"
                                   Margin="0,3,0,0"/>
                        <!-- Links -->
                        <ItemsControl ItemsSource="{Binding Links}" Margin="0,6,0,0">
                            <ItemsControl.ItemsPanel>
                                <ItemsPanelTemplate>
                                    <WrapPanel Orientation="Horizontal"/>
                                </ItemsPanelTemplate>
                            </ItemsControl.ItemsPanel>
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <Button Content="{Binding Label}"
                                            Tag="{Binding Url}"
                                            Style="{StaticResource LinkButton}"
                                            Click="LinkButton_Click"/>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </StackPanel>
                </Grid>
            </Border>
        </DataTemplate>

        <!-- Section template -->
        <DataTemplate x:Key="SectionTemplate" DataType="{x:Type models:ChecklistSection}">
            <Border Style="{StaticResource Card}" Margin="0,0,0,10">
                <StackPanel>
                    <!-- Section header -->
                    <Button Click="SectionHeader_Click"
                            Tag="{Binding}"
                            Background="Transparent"
                            BorderThickness="0"
                            Padding="0"
                            Cursor="Hand"
                            HorizontalContentAlignment="Stretch"
                            Margin="0,0,0,4">
                        <Grid>
                            <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                                <TextBlock Text="{Binding Icon}" FontSize="18" Margin="0,0,10,0"
                                           VerticalAlignment="Center"/>
                                <TextBlock Text="{Binding Title}"
                                           Style="{StaticResource SectionHeader}"
                                           VerticalAlignment="Center"/>
                                <TextBlock x:Name="SectionCount"
                                           Foreground="{DynamicResource MutedText}"
                                           FontSize="12"
                                           VerticalAlignment="Center"
                                           Margin="8,0,0,0"/>
                            </StackPanel>
                            <TextBlock Foreground="{DynamicResource MutedText}"
                                       FontSize="16"
                                       HorizontalAlignment="Right"
                                       VerticalAlignment="Center">
                                <TextBlock.Style>
                                    <Style TargetType="TextBlock">
                                        <Setter Property="Text" Value="▼"/>
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding IsExpanded}" Value="True">
                                                <Setter Property="Text" Value="▲"/>
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </TextBlock.Style>
                            </TextBlock>
                        </Grid>
                    </Button>

                    <!-- Items -->
                    <ItemsControl ItemsSource="{Binding Items}"
                                  ItemTemplate="{StaticResource ChecklistItemTemplate}"
                                  Visibility="{Binding IsExpanded, Converter={StaticResource BoolToVis}}"/>
                </StackPanel>
            </Border>
        </DataTemplate>

    </UserControl.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <Border Grid.Row="0" Style="{StaticResource Card}" Margin="20,20,20,10">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="8"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>
                <Grid Grid.Row="0">
                    <StackPanel>
                        <TextBlock Text="Pre-Stream Checklist" Style="{StaticResource SectionHeader}" FontSize="18"/>
                        <TextBlock x:Name="ProgressLabel"
                                   Text="0 / 19 tasks complete"
                                   Style="{StaticResource Muted}"
                                   Margin="0,3,0,0"/>
                    </StackPanel>
                    <Button Content="↺  Reset"
                            Style="{StaticResource SecondaryButton}"
                            HorizontalAlignment="Right"
                            VerticalAlignment="Center"
                            Padding="14,7"
                            Click="Reset_Click"/>
                </Grid>
                <ProgressBar x:Name="ProgressBar" Grid.Row="2" Margin="0,4,0,0"/>
                <TextBlock x:Name="ProgressPct"
                           Grid.Row="3"
                           Text="0%"
                           Style="{StaticResource Muted}"
                           HorizontalAlignment="Right"
                           Margin="0,3,0,0"/>
            </Grid>
        </Border>

        <!-- Sections -->
        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" Margin="20,0,20,20">
            <ItemsControl x:Name="SectionsControl"
                          ItemTemplate="{StaticResource SectionTemplate}"/>
        </ScrollViewer>
    </Grid>
</UserControl>
